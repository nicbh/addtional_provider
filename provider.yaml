rule-providers:
  customization:
    type: http
    url: "https://raw.githubusercontent.com/nicbh/addtional_provider/refs/heads/main/rules.yaml"
    interval: 2000000
    proxy: 🍃 Proxies
    behavior: classical
    format: yaml
    size-limit: 0

rules:
  - RULE-SET,customization,DIRECT

